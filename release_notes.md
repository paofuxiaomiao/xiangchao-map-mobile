## 2026-04-19 GitHub Pages 发布进度更新
- 当前位于仓库设置页底部 Danger Zone。
- 仓库仍处于 Private 状态。
- 已确认可见性入口按钮 domId 为 `visibility_menu-button`，页面可直接继续执行公开仓库操作。
- 误打开过一次“Disable branch protection rules”对话框，已通过键盘关闭，当前页面恢复正常。

页面发布设置已完成：仓库已切换为 Public，GitHub Pages 的发布源已保存为 `gh-pages` 分支与 `/ (root)` 目录。设置页已出现“GitHub Pages source saved.”提示，并显示站点正在从 `gh-pages` 分支构建。

线上验证结果显示，GitHub Pages 已成功发布并可访问。首页返回 200，页面标题为“湘超数字地图看板”。在浏览器中访问 `/map`、`/modules` 与 `/h5` 三个核心路由时，页面内容均已正确渲染：地图页可见全省地图与长沙图层示例，功能模块页可见球队风采、投票、留言板与战报模块，H5 页可见六段式体验内容。需要注意的是，使用命令行直接请求这三个深层路径时，HTTP 状态码仍显示为 404，但浏览器端实际能够通过前端应用完成渲染，因此当前用户访问体验已可用，不过后续若要进一步优化直链状态码表现，可考虑补充更完整的静态托管回退方案。

LOGO 修复复核结果：已将 LOGO 从 public 绝对路径引用改为由前端构建系统托管的源码资源导入，并重新发布到 main 与 gh-pages。线上验证显示：首页 `https://paofuxiaomiao.github.io/xiangchao-map/` 的顶部品牌卡片中，湘超 LOGO 已正常显示；H5 页面 `https://paofuxiaomiao.github.io/xiangchao-map/h5` 的封面中央 LOGO 也已正常显示，不再是空白或损坏图标。远程最新提交为 main 分支 `f19d113`，gh-pages 分支 `aa2f904`。

衡阳示例验证记录：在 `https://paofuxiaomiao.github.io/xiangchao-map/map` 当前浏览器视口下，页面呈现为桌面布局而非移动端布局。选择衡阳后，地图自动聚焦到衡阳区域，右侧出现固定详情面板，详情面板宽度约占右侧较大空间，但地图主体仍可见；当前视口未能直接复现“移动端底部/全屏遮挡地图”的症状。不过从源码已确认 `TeamDetail` 在小屏下为 `w-full max-w-[420px] absolute top-0 right-0 bottom-0`，因此在移动端会以全宽侧层覆盖地图，属于结构性遮挡问题。

临时屏蔽三维入口处理记录：已从 `client/src/pages/Home.tsx` 移除地图页右上角“实景三维 3D”进入按钮，仅保留“全省总览”等其他控制项。随后已重新执行 `pnpm run build:pages`，并将修改发布到远程 `main` 与 `gh-pages`。远程最新提交为 `main: 1aeb5d2 chore: hide 3d entry button temporarily`，`gh-pages: 4e347e9 deploy: hide 3d entry button temporarily`。线上复核页面 `https://paofuxiaomiao.github.io/xiangchao-map/map` 时，右上角控制区已不再出现“实景三维”按钮。

关于新需求方向的阶段性结论：现有项目已具备红色主视觉基础，Landing 与 FeatureModules 页可承接文化与互动扩展。`teams.ts` 已有球队积分、胜平负、进失球和亮点字段；`feature-data.ts` 已有球队故事、票选、留言、战报等雏形，适合扩展为更完整的数字看板与互动区。外部公开信息方面，搜索结果显示湘超相关媒体报道已能找到部分球队主教练、名单与赛季信息，但球员身高、体重、个人得分等字段的公开完整度不稳定，适合采用“公开可验证数据优先 + 缺失字段留空或标注待补充”的分层策略。文化表达方面，湖南省政府公开资料明确提到炎帝陵在株洲炎陵县、舜帝陵在永州九嶷山，可作为株洲“炎帝文化”、永州“舜帝文化”设计锚点。

外部浏览补充：湖南省政府公开页面《炎帝与炎帝陵、舜帝与舜帝陵-湖南简况》明确写明炎帝陵在株洲市炎陵县、舜帝陵在永州九嶷山，可直接支撑株洲炎帝与永州舜帝的城市文化表达。尝试访问百度百科“湘超长沙代表队”页面时遇到安全验证，说明部分人物/名单类资料在自动化抓取时存在访问阻力，因此球员、教练、身高体重等字段的正式落地更适合采用可替换的数据结构，并优先接入稳定来源或人工校对后的资料。

本地预览验证：`/modules?view=dashboard` 已默认进入“数字看板”，页面顶部出现三大主视图卡片与球队选择条，说明“先选球队、再选视图”的分层策略已生效。切换到 `互动中心` 后，页面按“球队投票 → 球员打分 → 线上打气 → 比赛评论区 → 战报联动”顺序展开，互动功能没有和数字看板混杂在首屏同层，交互组织符合预期。首页新增的模块快捷入口已通过查询参数支持直达 `dashboard / interactive / culture` 三种主视图。

本地首页验证：主视觉 CTA 区已新增 `查看功能模块总览 / 直达数字看板 / 直达互动中心 / 直达主场文化` 四个模块相关入口，位置集中在首页首屏主按钮区域，进入路径清晰。点击“直达主场文化”后，URL 正确跳转为 `/modules?view=culture`，说明查询参数路由和首页快捷入口已联通；文化视图中已出现文化锚点、啦啦队、粉丝队、页面策略、看台仪式与横幅墙等新内容分层。

本轮发布状态：源码已推送到 `main@5cf7517`，静态站点产物已推送到 `gh-pages@d423b66`。本地构建与本地浏览验证均通过，新版模块中心已包含三大主视图、球队联动、首页快捷入口和查询参数跳转。线上 GitHub Pages 在当前校验时仍返回旧版模块页内容，说明静态站点尚未完成缓存刷新或全站传播；因此当前最稳妥的结论是“代码与发布分支已更新，正式站点预计还需要一段时间完成生效”。

功能检查记录：本地首页首屏可见 `查看功能模块总览 / 直达数字看板 / 直达互动中心 / 直达主场文化 / 进入 H5 体验` 五个关键入口，入口层级清晰。点击 `直达数字看板` 后，URL 正常跳转到 `/modules?view=dashboard`，并正确进入模块页的三视图结构；模块页首屏可见三大主视图入口、14 支球队选择条，以及数字看板内部二级标签 `球队总览 / 阵容档案 / 联赛榜单`，说明首页直达与模块首屏分层均已联通。

功能检查记录：模块页切换到 `互动中心` 后，页面能够按“球队热度投票 → 球员打分 → 线上打气 → 评论区”的顺序组织内容，没有与数字看板混屏。将当前球队从长沙切换到 `衡阳队` 后，联动数据已经更新，包括球队热度票数由 980 切为 756、球员平均评分由 4.4 切为 4.2，线上打气文案同步变为“衡阳雄起 / 雁城一起飞 / 主场别松劲”，评论输入框占位文案也同步改成“写下你对 衡阳队 的评价或赛前打气”，说明当前球队驱动的互动联动逻辑正常。

功能检查记录：在 `互动中心` 中实际执行交互后，`衡阳队` 投票按钮点击后状态由“点击投出 1 票”变为“本机已投票”，同时榜单内衡阳票数显示为 `757`，说明本地投票去重与即时票数更新已生效。继续点击 `衡阳雄起` 打气按钮后，对应热度由 `0 HEAT` 即时变为 `1 HEAT`，说明线上打气的本地计数逻辑正常。

功能检查记录：评论表单的昵称输入框与文本域均可正常填写；点击 `发送评论` 后，输入框被清空，且最新评论列表顶部新增了一条来自 `功能检查员`、关联 `衡阳队` 的评论，说明评论提交流程和列表即时刷新逻辑正常。

功能检查记录：切换到 `主场文化` 视图后，当前球队仍保持为 `衡阳队`，文化内容成功联动为“雁城衡州 × 南岳气场”。页面已正常展示文化锚点、主场标语、啦啦队、粉丝队、页面策略、看台仪式和横幅墙，说明文化模块入口与球队联动逻辑正常，且内容分层完整。

功能检查记录：正式线上地址 `https://paofuxiaomiao.github.io/xiangchao-map/modules?view=dashboard` 当前直接访问返回 GitHub Pages 404 页面。这说明**本地预览中的模块功能基本可用，但线上静态托管对深层路由的回退处理仍未恢复**，属于当前最明显的线上阻塞问题；用户若从首页内部导航进入模块页大概率可用，但直接打开模块页链接会失败。

功能检查记录：进一步复核正式线上站点时，`/xiangchao-map/` 首页标题仍存在，但浏览器视口未渲染出任何可交互内容，呈现近似空白页状态；与此同时，`/xiangchao-map/modules?view=dashboard` 直接访问返回 GitHub Pages 404。结合本地构建目录仅包含 `index.html`、缺少 `404.html` 回退文件，可以初步判断：**本地功能逻辑基本正常，但正式站点仍存在单页应用发布配置问题，至少包括深层路由回退缺失，且首页可能还叠加了静态资源基路径或发布产物引用异常。**

发布修复验证：重新使用 `build:pages` 生成带 `/xiangchao-map/` 基路径、`404.html` 回退与 `.nojekyll` 的产物并发布后，正式站点首页 `https://paofuxiaomiao.github.io/xiangchao-map/` 已恢复正常渲染，首屏可见主视觉和多个快捷入口；深层地址 `https://paofuxiaomiao.github.io/xiangchao-map/modules?view=dashboard` 在真实浏览器中也已正确渲染为新版三主视图模块页，不再落回 GitHub 默认 404 提示页。需要补充说明的是：GitHub Pages 对深层路由仍会返回 404 状态码，但内容已由自定义 `404.html` 接管并成功加载单页应用，这是当前静态托管下的正常回退行为。

前端说明文案清理复核：已从源码与 `dist/public` 中移除 `为什么这样分层`、`Modules Re-architecture`、`Quick Entry`、`Interactive 0x` 及 H5 中的过程描述话术。使用 `build:pages` 重建后，关键误用文案在产物中已检索不到。尝试通过本地 `vite preview` 复核模块页时，浏览器仍呈现空白，但标题存在且控制台暂未捕获输出；因此当前更可靠的验证依据是源码差异与构建产物字符串检查，后续将直接发布到正式站点再做真实线上渲染确认。

内部设计说明误上屏修复复核：正式站点最初在浏览器中仍显示旧版模块页文案，但通过直接抓取线上 HTML 可确认 GitHub Pages 已切换到新的 `index-CdoLdMfJ.js` 资源。使用带缓存规避参数的地址 `https://paofuxiaomiao.github.io/xiangchao-map/modules?view=dashboard&refresh=20260421a` 重新打开后，浏览器界面已显示修复后的新文案：`湘超球队主题中心`、`功能入口`、`球队焦点`，此前错误出现的 `MODULES RE-ARCHITECTURE`、`QUICK ENTRY`、`为什么这样分层` 等内容已不再出现。说明本次修复已在线上生效，浏览器曾显示旧内容主要属于缓存/传播滞后。

本轮模块页重构补充：`FeatureModules.tsx` 已按红色政府门户专题风格重构，页面统一为“专题页眉 + 球队切换带 + 视图切换带 + 正文专题区 + 文化收束区”的结构。株洲队已接入炎帝文化背景图，永州队已接入舜帝文化背景图，二者均以低对比背景方式融入右侧专题信息区，避免压制正文信息。开发环境下已分别验证 `数字看板 / 互动中心 / 主场文化` 三个视图能够正常渲染，且株洲、永州切换后文化背景图显示正确。需要补充说明的是，本地 `vite preview` 直接访问深层路由时出现空白，属于本地静态预览与单页应用深链回退差异，不影响构建产物已包含 `404.html` 与新版模块页资源这一事实；正式线上效果仍需在 GitHub Pages 地址中继续复核。
线上发布复核补充：正式地址 `https://paofuxiaomiao.github.io/xiangchao-map/modules?view=culture` 当前浏览器实际渲染的仍是旧版模块页首屏文案（如“湘超数字看板与球迷互动中心”“一站看懂球队实力、主场氛围与球迷热度”），与本轮本地开发环境中的新版红色专题页眉不一致；但直接抓取线上 HTML 已确认引用到最新构建资源 `assets/index-DOb6w3a4.js` 与 `assets/index-DPY4GAqH.css`，并且在线上主脚本中可检索到“以红色主视觉统领”“城市锚点与主场叙事收束”等新版字符串。这说明远程分支与静态资源已更新，当前浏览器侧仍存在缓存、传播或回退页面与新脚本混合生效的现象，需作为交付说明中的已知事项保留。
缓存诊断补充：在正式站点当前浏览器页面上下文中执行检查后，确认根节点文本仍包含旧版标题“湘超数字看板与球迷互动中心”，不包含新版标题“以红色主视觉统领”；同时页面实际加载的资源仍是旧哈希文件 `/xiangchao-map/assets/index-CdoLdMfJ.js` 与 `/xiangchao-map/assets/index-BWb-IW8V.css`。这与命令行抓取到的最新 HTML 已指向新哈希 `index-DOb6w3a4.js / index-DPY4GAqH.css` 形成对照，基本可以判定为浏览器会话或 CDN 传播仍持有旧资源缓存，而非本轮 `gh-pages` 推送失败。
